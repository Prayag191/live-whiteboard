spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/whiteboard
    username: postgres
    password: root   # ← yaha apna real password daalo

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080